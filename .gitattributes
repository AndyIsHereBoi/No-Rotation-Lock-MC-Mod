# Keep the Gradle wrapper POSIX script LF-only so Linux CI runners can execute it.
# A CRLF gradlew fails with: bad interpreter: /bin/sh^M
gradlew text eol=lf
gradlew.bat text eol=crlf

# Gradle wrapper jar is binary; never normalize it.
*.jar binary
